import 'package:flutter/material.dart';

class Ddeals extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Deals Clicked")),
    );
  }
}
